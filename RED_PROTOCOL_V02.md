# RED PROTOCOL v0.2 — Local Conversational Agent

这个分支把 v0.1 的固定选项状态机改成自由输入的本地语言模型对话代理。

## 运行结构

- `index.html`：手机/电脑浏览器聊天界面、角色设置、关系状态、本地会话记忆。
- `server.py`：零第三方 Python 依赖的本地 HTTP relay；负责静态网页和 `/api/chat`。
- Ollama：真正负责语言理解、角色生成和上下文推理。

浏览器本身没有语言模型。只打开 GitHub Pages 或单独打开 `index.html` 不会产生真正的 AI 对话，必须让 `server.py` 能访问正在运行的 Ollama。

## 最短启动方式

1. 在电脑安装并启动 Ollama。
2. 拉取一个可用的聊天模型，例如：`ollama pull qwen3:8b`
3. 在这个分支目录运行：`python server.py`
4. 电脑浏览器打开：`http://127.0.0.1:8765`
5. iPhone 与电脑在同一局域网时，打开：`http://电脑局域网IP:8765`

默认服务监听 `0.0.0.0:8765`，Ollama 地址默认是 `http://127.0.0.1:11434`。

可通过环境变量覆盖：

- `RED_PROTOCOL_HOST`
- `RED_PROTOCOL_PORT`
- `OLLAMA_BASE_URL`

## v0.2 与 v0.1 的根本区别

v0.1：按钮 -> 预写分支 -> 固定状态变化。

v0.2：自由文本 -> 角色代理读取上下文与状态 -> 本地模型生成自然回应 -> 返回轻量状态增量与连续性记忆。

角色可以自然追问、拒绝、改变节奏、记住前文和描述自己的动作/环境；默认不提供 A/B/C 菜单，也不替用户决定动作、感受或同意。

## 会话记忆

浏览器保存：

- 当前角色配置
- 最近聊天记录
- trust / control / stress
- 模型生成的精简连续性记忆

默认保存在当前浏览器的 `localStorage`，不会由这个程序主动上传到远端。

## 安全规则

该版本仅用于成年人、自愿、虚构、非露骨的角色互动。红/黄/绿被作为协议事件传给模型；硬边界在每一轮都进入系统提示词。模型的私有推理不会显示在 UI 中，只显示最终角色回应与机器可用的状态字段。
